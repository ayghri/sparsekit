[project]
name = "sparsekit"
version = "0.1.0"
description = "Sparsity Kit for Structured Sparsity Specification"
authors = [
    {name = "Ayoub G.", email = "research@ayghri.me"}
]
readme = "README.md"
requires-python = ">=3.10,<3.15"

[tool.setuptools]
packages = ["sparsekit"]

[tool.poetry.dependencies]
torch = "^2.10.0"
